---
---
<section class="pt-32 pb-20 px-6 bg-gradient-to-b from-slate-900 to-slate-800" itemscope itemtype="https://schema.org/WebPageElement">
  <div class="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 items-center">
    <div class="space-y-6">
      <h1 class="text-5xl md:text-6xl font-bold leading-tight text-slate-100" itemprop="headline">
        Don't let <span class="text-blue-400">"canned responses"</span> kill your LINE OA conversion rate
      </h1>
      <p class="text-lg md:text-xl text-slate-300 leading-relaxed" itemprop="description">
        Integrate with Live Chat, LINE OA, and WellyTalk. AI automatically reviews every text response, accurately intercepts careless replies and emotional outbursts, making text customer service warm and human.
      </p>
      <div class="flex flex-wrap gap-4 pt-4">
        <a href="#contact" class="px-8 py-4 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 transition-all text-base md:text-lg shadow-lg shadow-blue-500/20">
          Free Integration Test
        </a>
        <a href="#integration" class="px-8 py-4 border-2 border-slate-600 text-slate-200 rounded-lg font-medium hover:bg-slate-800 hover:border-slate-500 transition text-base md:text-lg">
          View Integrations
        </a>
      </div>
    </div>

    <div class="relative">
      <div class="bg-slate-800 rounded-2xl shadow-2xl p-6 border border-slate-700">
        <div class="flex items-center gap-3 mb-6 pb-4 border-b border-slate-700">
          <div class="w-10 h-10 bg-green-500/20 rounded-full flex items-center justify-center text-green-400 font-bold border border-green-500/30" aria-label="LINE icon">
            L
          </div>
          <div>
            <div class="font-semibold text-slate-200">LINE Official Account</div>
            <div class="text-xs text-slate-400">Monitoring in real-time</div>
          </div>
        </div>

        <div class="space-y-4" id="chatDemo">
          <div class="flex justify-start">
            <div class="max-w-[80%]">
              <div class="p-3 rounded-2xl bg-slate-700 text-slate-200">
                Can I return this item?
              </div>
            </div>
          </div>
          <div class="flex justify-end">
            <div class="max-w-[80%]">
              <div class="p-3 rounded-2xl bg-slate-700 border-2 border-orange-500/50 text-slate-200">
                No, it's already sold. Why would you return it?
              </div>
              <div class="mt-2 flex items-start gap-2 text-xs text-orange-400 bg-orange-500/10 p-2 rounded-lg border border-orange-500/30">
                <svg class="w-3.5 h-3.5 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                </svg>
                <span class="font-medium">Impatient tone detected</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="absolute -right-4 -top-4 bg-blue-600 text-white px-4 py-2 rounded-lg shadow-lg flex items-center gap-2">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
        </svg>
        <span class="text-sm font-medium">AI Real-time Monitoring</span>
      </div>
    </div>
  </div>
</section>

<script>
  // Simple chat animation
  const messages = [
    { text: "Can I return this item?", sender: "customer" },
    { text: "No, it's already sold. Why would you return it?", sender: "agent", warning: true }
  ];
  
  let currentMessage = 0;
  const chatDemo = document.getElementById('chatDemo');
  
  // Animation logic can be added here
</script>

<style>
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.5;
    }
  }
  
  .animate-pulse {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
</style>

